这里，直接一键全选复制 → 新建文件 → 命名为 `day5_学习笔记.md` → 粘贴保存 → git push 即可！

```markdown
# Day 5 - 列表高级操作 + 完整学生成绩管理系统

**日期**：2025年11月19日  
**目标**：彻底掌握 Python 列表操作 + 完成第一个真正可用的完整工具程序  
**仓库**：https://github.com/souer25/study  
**主文件**：day5_complete.py

## 一、列表核心操作速查表

| 操作           | 代码示例                          | 说明                              |
|----------------|-----------------------------------|-----------------------------------|
| 创建           | `lst = [1, 2, 3]`                |                                   |
| 访问           | `lst[0]`, `lst[-1]`              | 支持负索引                        |
| 切片           | `lst[1:4]`, `lst[:3]`, `lst[::2]`| 左闭右开，可加步长                |
| 添加           | `lst.append(x)` / `lst.insert(i,x)` | 末尾或指定位置                  |
| 删除           | `lst.remove(x)` / `lst.pop(i)`   | remove删值，pop删索引并返回       |
| 判断存在       | `x in lst`                       | 返回 True/False                   |
| 找索引         | `lst.index(x)`                   | 找不到会报错！记得先 in 判断    |
| 排序（原地）   | `lst.sort(reverse=True)`         | 直接改原列表                      |
| 排序（新列表） | `sorted(lst, reverse=True)`      | 原列表不变                        |
| 长度           | `len(lst)`                       |                                   |

## 二、今日五大神级技巧（以后必用）

1. **zip()** —— 拉链配对两个列表  
   ```python
   list(zip(scores, names))
   # → [(88, '张三'), (92, '李四'), ...]
   ```

2. **enumerate(..., start=1)** —— 自动加上排名  
   ```python
   for rank, item in enumerate(lst, 1):
       print(rank, item)
   ```

3. **元组解包**（超级优雅）  
   ```python
   for rank, (score, name) in enumerate(combined, 1):
       print(rank, name, score)
   ```

4. **try-except + while True** —— 防御性输入校验（防止程序崩溃）  
   ```python
   while True:
       try:
           score = int(input("成绩: "))
           if 0 <= score <= 100:
               break
           else:
               print("必须0-100！")
       except ValueError:
           print("请输入纯数字！")
   ```

5. **f-string 对齐输出** —— 让控制台输出像表格一样整齐  
   ```python
   print(f"{'排名':<4}{'姓名':<10}{'成绩':<5}")
   print(f"{rank:<4}{name:<10}{score:<5}")
   ```

## 三、项目：学生成绩管理系统（纯列表版）

数据结构（最简单但够用）：
```python
names  = []   # 姓名
scores = []   # 成绩（索引一一对应）
```

已完整实现 7 大功能：
1. 添加学生（带范围校验）
2. 删除学生（按姓名）
3. 修改成绩（反复输入直到合法）
4. 查询学生
5. 排行榜（降序 + 排名 + 美观对齐）
6. 统计（平均分、最高、最低、及格人数、及格率）
7. 退出

核心代码亮点：
- 修改成绩：`names.index(name)` + `try-except` + `while True` + `break`
- 排行榜：`zip` → `sort(reverse=True)` → `enumerate(...,1)` → 元组解包 → f-string 对齐

## 四、今日最大收获

- 能独立写出带菜单、增删改查、排序统计、防呆的完整工具
- 学会了“防御性编程”——专业程序员的必备思维
- 代码不再是乱七八糟的输出，而是整齐美观的表格
- 第一次感受到“哇，我写的程序真好用！”的成就感

**Day 5 彻底拿下！**  
下一个目标：Day 6 用字典 + JSON 实现数据永久保存（关程序重启还在）**

30天速成计划 · Day 5 ✓  继续冲！🚀
```

全选 → 复制 → 新建 day5_学习笔记.md → 粘贴 → 保存 → 三行命令推上去：

```bash
git add day5_学习笔记.md
git commit -m "add: day5 完整学习笔记"
git push
```

推完直接回复我“笔记已上传”，我们立刻 Day6 起飞！  
你现在的节奏已经非常猛了，继续保持！