## 📅 Python 学习笔记 - 第 1 天

### 目标：Python 基础入门 - 变量、数据类型与控制流

#### 1. 变量 (Variables) 与数据类型 (Data Types)

| 概念 | 作用 | 示例 (Python) |
| :--- | :--- | :--- |
| **变量** | 存储数据的“容器”，用名称引用值。 | `age = 25` |
| **int** | 整数类型（无小数点）。 | `num = 100` |
| **float** | 浮点数类型（带小数点）。 | `pi = 3.14159` |
| **str** | 字符串类型（文本），**必须用引号包围**。 | `name = "Gemini"` |
| **bool** | 布尔类型（逻辑判断值），只有 `True` 或 `False`。 | `is_learning = True` |

---

#### 2. 运算符 (Operators)

| 类型 | 作用 | 常用符号 | 重点回顾 |
| :--- | :--- | :--- | :--- |
| **算术** | 进行数学计算。 | `+`, `-`, `*`, `/` | `//` (整除), `%` (取余) |
| **赋值** | 将值存储到变量中。 | `=` | `+=` (复合赋值，如 `x += 1`) |
| **比较** | 比较两个值。 | `==`, `!=`, `>`, `<` | **注意：** 判断是否相等要用**双等号 `==`**。 |

---

#### 3. 控制流 (Control Flow)

**A. 条件语句 (`if/elif/else`)**

* **作用**：让程序根据条件进行决策和分支执行。
* **关键点**：条件判断的结果必须是布尔值 (`True` 或 `False`)。
* **结构**：
    ```python
    if condition1:
        # 如果 condition1 为 True
        pass
    elif condition2:
        # 否则，如果 condition2 为 True
        pass
    else:
        # 否则 (所有条件都不满足)
        pass
    ```

**B. 循环语句 (`for` 循环)**

* **作用**：重复执行一段代码块，常用于**遍历**序列元素或执行**固定次数**。
* **遍历序列**：`for item in sequence: ...`
* **固定次数**：
    * `range(N)`：生成 $0$ 到 $N-1$ 的整数，共 $N$ 个数。
    * **回顾**：要循环 10 次（第 1 天到第 10 天），应该使用 `range(10)`，并通过 `i + 1` 来调整打印结果。

---

### 🚀 学习表现分析

你今天在以下几个方面表现**非常出色**：

* **理解赋值与比较的区别**：虽然最初混淆了 `=` 和 `==`，但在修正后迅速理解并应用于条件判断中。
* **掌握控制流逻辑**：编写的成绩评估代码（`if/elif/else`）逻辑严密，展示了对条件语句的精通。
* **运用 `range()`**：理解了 `for i in range(N)` 从 $0$ 开始计数，并懂得用 `i + 1` 来满足实际需求。