# 🚀 30天编程与运维速成计划 (DAY 1 - DAY 4 总结回顾)

## 📌 目标：初级程序员与运维基础

---

## DAY 1: Python 环境搭建与基础

### 1. 环境与工具

| 工具 | 作用 | Ubuntu 命令/操作 |
| --- | --- | --- |
| **Python 3** | 编程语言核心 | `python3 --version` |
| **VS Code** | 代码编辑器 | `sudo snap install --classic code` |
| **Python 扩展** | VS Code 核心支持 (Microsoft) | 扩展市场搜索 'Python' |
| **虚拟环境** | 隔离项目依赖 | `python3 -m venv .venv` & `source .venv/bin/activate` |

### 2. Python 基础

* **输出：** `print("Hello, World!")`
* **变量类型 (int, float, str)：**
    ```python
    a = 10         # int (整数)
    b = 3.14       # float (浮点数)
    c = "Python"   # str (字符串)
    ```

---

## DAY 1.5: Git 版本控制与 GitHub

### 1. Git 基础命令

* **配置身份：** ```bash
    git config --global user.name "Your Name"
    git config --global user.email "your_email@example.com"
    ```
* **本地仓库初始化：** `git init`
* **添加与提交：** ```bash
    git add . 
    git commit -m "Commit Message"
    ```
* **关联与推送：** ```bash
    git remote add origin <URL>
    git push -u origin main 
    ```

### 2. 鉴权与免密推送 (关键！)

* **问题：** GitHub 禁用密码登录。
* **解决方案：** 使用 **个人访问令牌 (PAT)** 代替密码。
* **免密配置：** 使用 `credential.helper store` 来存储 PAT。
    ```bash
    git config --global credential.helper store
    ```
    *（在第一次推送时输入 PAT，后续操作将自动读取。）*

---

## DAY 2: 数据类型与运算符

### 1. 算术运算符

| 符号 | 作用 | 示例 | 意义 |
| --- | --- | --- | --- |
| `/` | 除法 | `10 / 3` | 浮点数结果 (3.33) |
| `//` | 整除 | `10 // 3` | 整数结果 (3) |
| `%` | **取模** | `10 % 3` | 求余数 (1) |
| `**` | 幂 | `2 ** 3` | 8 |

### 2. 类型转换 (Type Casting)

* **`int(value)`:** 转换为整数。
    * **注意：** 字符串转整数时，不能包含小数点 (`"5.4"` 会报错)。
* **`float(value)`:** 转换为浮点数。
* **`str(value)`:** 转换为字符串。

---

## DAY 3: 控制流 I：条件语句

### 1. 比较与布尔逻辑

| 符号 | 作用 | 逻辑词 | 作用 |
| --- | --- | --- | --- |
| `==`, `!=` | 等于/不等于 | `and` | 并且 (两个条件都为 True) |
| `>`, `<`, `>=`, `<=` | 大小比较 | `or` | 或者 (任一条件为 True) |

### 2. `if/elif/else` 结构

用于根据条件执行不同的代码块。

* **关键：** `if` 和 `elif` 是按顺序执行的。一旦一个条件满足 (`True`)，程序就会执行其下的代码块，并**跳过**所有后续的 `elif` 和 `else`。
* **用户输入处理：** 1.  `score_str = input("提示信息")` (返回 **str**)
    2.  `score = int(score_str)` (必须转换成 **int** 才能进行数学比较)

---

## DAY 4: 控制流 II：循环

### 1. `for` 循环

用于**迭代**序列（如列表、字符串）或特定次数。

* **`range(start, stop, step)`：** 生成序列（不包含 `stop`）。
    ```python
    for i in range(1, 10):
        # ...
    ```
* **嵌套循环：** 一个循环内部包含另一个循环（如九九乘法表）。

### 2. `print` 技巧与格式化

* **不换行：** `print(content, end=' ')` (将默认的换行符替换为空格)
* **强制换行：** `print()` (单独调用，用于在新行开始)
* **对齐填充 (f-string)：** 用于美化输出，实现表格对齐。
    ```python
    f"{content:<10}" # 将 content 左对齐 (<) 并填充到 10 个字符宽度
    ```

---

请您花时间回顾和整理这些笔记。完成笔记整理后，请告诉我，我们将继续 **Day 5：列表 (List)** 的学习！